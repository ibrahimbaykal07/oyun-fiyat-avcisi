name: Oyun Listesi Robotu

on:
  schedule:
    # UTC 04:00 = Türkiye Saati ile 07:00
    - cron: '0 4 * * *'
  workflow_dispatch: # Manuel tetikleme butonu

permissions:
  contents: write

jobs:
  update-games:
    runs-on: ubuntu-latest
    
    steps:
      - name: Depoyu Çek
        uses: actions/checkout@v3

      - name: Python Kur
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: Robotu Çalıştır
        run: python update_bot.py

      - name: Kaydet ve Gönder
        run: |
          git config --global user.name 'OyunFiyatBotu'
          git config --global user.email 'bot@noreply.github.com'
          git add subscriptions.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 PCGW: Liste güncellendi" && git push)
